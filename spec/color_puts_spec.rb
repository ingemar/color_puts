require 'spec_helper'

describe ColorPuts do
  it 'has a version number' do
    expect(ColorPuts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
